A D R zero zero two documents the integration choice: reuse the Python Q U B O compiler in a subprocess instead of rewriting it in Kotlin first.

The R C A agent writes an instance J S O N, runs solve json dot p y, reads one J S O N line from standard out, and attaches findings under qubo when the feature flag is on.

To prove the bridge without Datadog or an L L M, run scripts slash qubo e two e smoke dot sh from the repository root. It creates a virtual environment under dice leap poc, installs the package, runs the toy fixture, then runs Dice Leap Python Solver Integration Test with Maven.

Export P Y T H O N to the venv interpreter so Maven sees the same Python as your shell.

Local classical solving stays the default for demos and C I; Leap hybrid remains an explicit opt in when you are ready for cloud hybrid samplers.
