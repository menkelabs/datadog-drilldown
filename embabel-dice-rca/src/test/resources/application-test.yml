spring:
  application:
    name: embabel-dice-rca-test
  main:
    banner-mode: off

# Disable real Datadog in tests
datadog:
  api-key: ""
  app-key: ""
  site: datadoghq.com

# Dice configuration for tests
dice:
  ingestion:
    enabled: true
    batch-size: 10
  analysis:
    auto-trigger: true

# Logging - quieter for tests
logging:
  level:
    root: WARN
    com.example.rca: INFO
